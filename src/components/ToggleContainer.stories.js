import React from 'react';
import ToggleContainer from './ToggleContainer';

export default {
  component: ToggleContainer,
  title: 'ToggleContainer',
}

const Template = (args) => <ToggleContainer {...args} />;

export const Default = Template.bind({});

Default.args = {
  
}